# Suggested Commands

- Prefix repository shell commands with `rtk` per the user's global Codex rules.
- Inspect state: `rtk git status --short`.
- Inspect remotes: `rtk git remote -v`.
- No dev, build, lint, typecheck, or test command exists yet; add exact project commands after the initial scaffold is committed.
- Current completion checks are limited to a fresh Git status/diff plus validation of any documentation written by the active setup task.